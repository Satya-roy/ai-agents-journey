# GitHub Profile Summariser

Fetches a GitHub user's repos and prints a structured summary.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # add your GitHub token
```

## Run
```bash
python main.py
```
